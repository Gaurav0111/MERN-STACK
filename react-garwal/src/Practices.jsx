export const Practices = () => {
    const student = [];
    return (
        <>
            <p>{student.length && "No Student Found"} </p>
            <p>Number of Student: {student.length} </p>
        </>
    )
}